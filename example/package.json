{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd .. && yarn build && cd example && recife start",
    "server": "cd .. && yarn build && cd example && recife server",
    "build": "cd .. && yarn build && cd example && recife build"
  },
  "dependencies": {
    "recife": "^0.4.0",
    "recife-koa": "^0.1.2",
    "typescript": "^3.9.5"
  }
}
